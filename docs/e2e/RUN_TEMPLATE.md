# CodyWork E2E 回归记录：<功能或版本>

- 日期：
- 执行者：
- 代码 revision：
- Core 版本：
- 环境与 URL：
- 浏览器与视口：
- SQLite 数据库：临时 / 正式只读验证
- 测试 Workspace：

## 结论

填写整体结论，以及是否允许继续提交、合并或部署。

## 用例结果

| 用例 | 结果 | 证据 | 备注 |
| --- | --- | --- | --- |
| E2E-001 | NOT RUN |  |  |

## 发现并修复的问题

记录现象、根因、修改范围，以及修改后重新执行了哪些用例。没有则写“无”。

## 未解决问题与阻塞

记录所有 `FAIL`、`BLOCKED` 和未执行的必要用例。没有则写“无”。

## 健康检查

- `pnpm verify`：
- 浏览器 console error/warn：
- 服务端异常日志：
- 刷新/重连后的最终状态：

## 清理

列出删除的一次性 Workspace、Worktree、临时数据库、上传文件和停止的测试进程。确认没有修改或清理真实用户仓库。
